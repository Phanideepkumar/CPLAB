#include<stdio.h>
int main () {
int i,sum=0,avg=0,marks[5];
printf("enter the marks : ");
for(i=0;i<5;i++) {
    scanf("%d",&marks[i]);
    }
for(i=0;i<5;i++) {
    sum=sum+marks[i];
    }
  printf("sum=%d",sum);
  avg=sum/5;
  printf("\navg=%d",avg);
  return 0;
  }
       
