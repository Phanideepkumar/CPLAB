#include<stdio.h>
#include<math.h>
int main () {
int a,b,c;
float s,area;
printf("\nenter the values of sides :");
scanf("%d%d%d",&a,&b,&c);
s=(a+b+c)/2 ;
printf("\ns=%f",s);
area=sqrt(s*(s-a)*(s-b)*(s-c));
 
 printf("\narea using heron's formula = %f",area);
 }
